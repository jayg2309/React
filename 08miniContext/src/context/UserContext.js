import React from "react";

const userContext = React.createContext();

export default userContext;

// steps
// 1. create a context
// 2. provide the context (create provider) -> to access and share data
// 3.  make sure all are aware of the context to access and share data
